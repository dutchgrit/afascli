name: .NET Core

on:
  push:
    branches: [ master ]

defaults:
  run:
    working-directory: src  
    
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Set Version 
      run: echo "::set-env name=testparam::lucas"
    - name: Test Verion 
      run: echo $testparam
      
    - name: Hello world
      run: echo Hello world $FIRST_NAME $middle_name $Last_Name $GITHUB_RUN_NUMBER
      env:
        #FIRST_NAME: $GITHUB_RUN_NUMBER  werkt NIET
        FIRST_NAME: lucas
        middle_name: '1.0.$GITHUB_RUN_NUMBER'
        Last_Name: ${{ format('1.0.{0}', env.GITHUB_RUN_NUMBER) }}
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
        key: installnet
    #- name: Install dependencies
    #  run: dotnet restore
    
   # - name: Build
   #   run: dotnet build --configuration Release --no-restore
      
   # - name: Pack
   #   run: dotnet pack --configuration Release
